#include<stdio.h>
int main()
{
    int num,reversed=0,digit;
    scanf("%d",&num);
    while(num!=0){
        digit=num%10;
        reversed=reversed*10+digit;
        num=num/10;
    }
    printf("the reversed number is %d",reversed);
    return 0;
}
