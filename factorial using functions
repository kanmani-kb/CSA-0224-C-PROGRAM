#include<stdio.h>
int factorial(int n);
int main(){
    int n,fact;
    scanf("%d",&n);
    fact=factorial(n);
    printf("factorial=%d",fact);
    return 0;
}
int factorial(int n){
int i,f=1;
for(i=1;i<=n;i++){
f=f*i;
}
return f;
}
